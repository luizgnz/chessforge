# Password material comes from Secret chessforge-db (ESO <- Vault), not from Git.
auth:
  username: chessforge
  database: chessforge
  existingSecret: chessforge-db
  secretKeys:
    userPasswordKey: password
    adminPasswordKey: postgres-password
primary:
  persistence:
    enabled: true
    size: 1Gi
